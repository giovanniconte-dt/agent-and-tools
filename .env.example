# Configurazione SQL Server (come ticket-classifier)
# ODBC Driver 17 for SQL Server
# Scegli UNO dei due metodi di autenticazione

# Metodo 1: Windows Authentication (Trusted Connection)
SQL_SERVER_DRIVER=ODBC Driver 17 for SQL Server
SQL_SERVER=localhost
SQL_DATABASE=nome_database
SQL_TRUSTED_CONNECTION=yes

# Metodo 2: SQL Server Authentication
# SQL_SERVER_DRIVER=ODBC Driver 17 for SQL Server
# SQL_SERVER=localhost
# SQL_DATABASE=nome_database
# SQL_USERNAME=tuo_username
# SQL_PASSWORD=tua_password
# SQL_TRUSTED_CONNECTION=no

# Se il server usa una porta diversa dalla 1433:
# SQL_SERVER=host,porta
# oppure SQL_PORT=1433

# Override: URL completo (se impostato, ignora le variabili sopra)
# MSSQL_CONNECTION_STRING=mssql+pyodbc://user:password@localhost:1433/mydb?driver=ODBC+Driver+17+for+SQL+Server

# Per test locali senza MSSQL (SQLite, no ODBC driver):
# MSSQL_CONNECTION_STRING=sqlite:///./test.db

# Ollama model (default: llama3.2)
OLLAMA_MODEL=llama3.2

# Flask API (optional)
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5000
# FLASK_DEBUG=false
